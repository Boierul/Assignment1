using System.Collections;

namespace Assignment1.Models
{
    public enum HairColors 
    {
        Black,
        Brown,
        Blonde,
        Ginger
        // @foreach (HairColors colors in Enum.GetValues(typeof(HairColors)))

    }
}